---
phase: 04-platform-channels
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/channels/sms.channel.ts
  - src/queue/workers/followup.worker.ts
  - src/queue/jobs/followup.job.ts
autonomous: true
requirements: [CHAN-02]

must_haves:
  truths:
    - "SMS follow-up messages are sent after WhatsApp follow-up sequence completes"
    - "SMS channel uses a provider API to deliver text messages"
    - "SMS follow-up only fires if lead has not responded after max WhatsApp follow-ups"
  artifacts:
    - path: "src/channels/sms.channel.ts"
      provides: "SMS IChannel implementation using Twilio/Vonage API"
      exports: ["SmsChannel"]
    - path: "src/queue/workers/followup.worker.ts"
      provides: "Extended follow-up worker that escalates to SMS after WhatsApp max"
  key_links:
    - from: "src/queue/workers/followup.worker.ts"
      to: "src/channels/sms.channel.ts"
      via: "channelManager.getChannel('sms') after MAX_FOLLOWUPS reached"
      pattern: "channelManager.*sms"
    - from: "src/channels/sms.channel.ts"
      to: "Twilio API"
      via: "axios or twilio SDK POST"
      pattern: "twilio|api\\.twilio\\.com"
---

<objective>
Add SMS channel integration and extend the follow-up worker to send SMS messages after the WhatsApp follow-up sequence is exhausted.

Purpose: Reach unresponsive leads via SMS as a final follow-up channel after WhatsApp attempts fail.
Output: SMS channel implementation, extended follow-up worker with SMS escalation.
</objective>

<execution_context>
@/Users/softsales/.claude/get-shit-done/workflows/execute-plan.md
@/Users/softsales/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/queue/workers/followup.worker.ts
@src/queue/jobs/followup.job.ts
@src/channels/channel.interface.ts

<interfaces>
From src/channels/channel.interface.ts (created in Plan 02):
```typescript
export type ChannelType = 'whatsapp' | 'instagram' | 'sms';
export interface IChannel {
  type: ChannelType;
  sendText(to: string, text: string, context?: Record<string, string>): Promise<void>;
  sendMedia(to: string, mediaUrl: string, caption?: string, context?: Record<string, string>): Promise<void>;
}
```

From src/queue/jobs/followup.job.ts:
```typescript
export interface FollowupJobData {
  leadId: string;
  instance: string;
  phoneNumber: string;
  followupNumber: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SMS channel implementation (Twilio)</name>
  <files>src/channels/sms.channel.ts, src/channels/channel.manager.ts, src/config/env.ts</files>
  <action>
1. Install twilio SDK: `npm install twilio` in backend root.

2. Add TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, TWILIO_PHONE_NUMBER to EnvConfig (all optional — system works without SMS configured).

3. Create src/channels/sms.channel.ts implementing IChannel:
   - type = 'sms'
   - Lazy-init Twilio client from env vars (TWILIO_ACCOUNT_SID + TWILIO_AUTH_TOKEN)
   - sendText: calls twilio.messages.create({ body: text, from: TWILIO_PHONE_NUMBER, to: phoneNumber }). Phone number must be in E.164 format. Brazilian numbers: if `to` doesn't start with +, prepend +55.
   - sendMedia: calls twilio.messages.create({ body: caption, from: TWILIO_PHONE_NUMBER, to: phoneNumber, mediaUrl: [mediaUrl] })
   - If Twilio env vars not configured, log warning and skip silently (no crash)

4. Update src/channels/channel.manager.ts to register 'sms' -> SmsChannel in the factory.
  </action>
  <verify>
    <automated>cd "/Users/softsales/Documents/SEVYN LABS/CODE/autoscar-agent" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>SmsChannel implements IChannel using Twilio SDK, channelManager returns it for type='sms', graceful skip when Twilio not configured.</done>
</task>

<task type="auto">
  <name>Task 2: Extend follow-up worker with SMS escalation after WhatsApp sequence</name>
  <files>src/queue/workers/followup.worker.ts, src/queue/jobs/followup.job.ts</files>
  <action>
1. Update FollowupJobData to include optional `channel` field (default 'whatsapp').

2. Modify followup.worker.ts logic:
   - Current behavior: sends up to MAX_FOLLOWUPS=2 WhatsApp messages with 48h delay
   - New behavior: after MAX_FOLLOWUPS WhatsApp attempts (followupNumber > MAX_FOLLOWUPS), schedule ONE SMS follow-up:
     ```
     followupNumber 1: WhatsApp message "Oi, tudo bem? Vi que voce se interessou..."
     followupNumber 2: WhatsApp message "Ainda tem interesse no veiculo?"
     followupNumber 3: SMS message "Ola! Vi seu interesse em um veiculo na Autoscar. Responda aqui ou no WhatsApp..."
     ```
   - For SMS follow-up (followupNumber > MAX_FOLLOWUPS): use channelManager.getChannel('sms').sendText() instead of WhatsApp
   - Only schedule SMS if TWILIO_ACCOUNT_SID is configured (check env var, skip if not set)
   - Do NOT schedule another follow-up after SMS (it's the final attempt)
   - Import channelManager from the channels module

3. SMS message template should be short (160 chars) and include the vehicle of interest if available (load lead data from Prisma to get vehicleUrl).
  </action>
  <verify>
    <automated>cd "/Users/softsales/Documents/SEVYN LABS/CODE/autoscar-agent" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Follow-up worker sends 2 WhatsApp follow-ups then 1 SMS follow-up. SMS only fires if Twilio is configured. No more follow-ups after SMS.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit`
2. SmsChannel correctly implements IChannel interface
3. Follow-up sequence: WhatsApp x2 -> SMS x1 -> stop
4. System does not crash when Twilio env vars are missing
</verification>

<success_criteria>
- SMS channel sends messages via Twilio API
- Follow-up worker escalates to SMS after WhatsApp sequence exhausted
- Graceful degradation when SMS not configured (no crash, log warning)
</success_criteria>

<output>
After completion, create `.planning/phases/04-platform-channels/04-03-SUMMARY.md`
</output>
