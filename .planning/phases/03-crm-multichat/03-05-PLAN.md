---
phase: 03-crm-multichat
plan: 05
type: execute
wave: 3
depends_on: ["03-03", "03-04"]
files_modified:
  - frontend/src/hooks/useSocket.ts
  - frontend/src/components/kanban/KanbanBoard.tsx
  - frontend/src/components/inbox/ChatWindow.tsx
  - frontend/src/app/inbox/page.tsx
autonomous: false
requirements: [CRM-07]

must_haves:
  truths:
    - "When a lead is moved by another user or the agent, the Kanban board updates without page refresh"
    - "When a new message arrives, the inbox conversation list and chat window update in real time"
    - "When human handoff is toggled, the UI reflects the change immediately for all connected clients"
  artifacts:
    - path: "frontend/src/hooks/useSocket.ts"
      provides: "Custom hook for Socket.IO event subscription with TanStack Query invalidation"
      exports: ["useSocket"]
  key_links:
    - from: "frontend/src/hooks/useSocket.ts"
      to: "frontend/src/lib/socket.ts"
      via: "imports getSocket singleton"
      pattern: "import.*getSocket.*socket"
    - from: "frontend/src/hooks/useSocket.ts"
      to: "@tanstack/react-query"
      via: "useQueryClient for invalidation on socket events"
      pattern: "useQueryClient"
    - from: "frontend/src/components/kanban/KanbanBoard.tsx"
      to: "frontend/src/hooks/useSocket.ts"
      via: "subscribes to lead:updated, lead:moved events"
      pattern: "useSocket"
---

<objective>
Wire Socket.IO real-time events into the frontend so CRM and inbox update live without polling. Verify the complete Phase 3 with a human checkpoint.

Purpose: CRM-07 requires real-time updates via WebSocket. The Socket.IO server (Plan 01) emits events on every mutation. This plan connects the frontend to those events using TanStack Query cache invalidation.

Output: useSocket hook, real-time updates across Kanban and inbox, verified end-to-end
</objective>

<execution_context>
@/Users/softsales/.claude/get-shit-done/workflows/execute-plan.md
@/Users/softsales/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-crm-multichat/03-RESEARCH.md
@.planning/phases/03-crm-multichat/03-01-SUMMARY.md
@.planning/phases/03-crm-multichat/03-02-SUMMARY.md
@.planning/phases/03-crm-multichat/03-03-SUMMARY.md
@.planning/phases/03-crm-multichat/03-04-SUMMARY.md

@frontend/src/lib/socket.ts
@frontend/src/lib/api.ts
@frontend/src/components/kanban/KanbanBoard.tsx
@frontend/src/components/inbox/ChatWindow.tsx

<interfaces>
<!-- Socket.IO events emitted by backend (from Plan 01 routes) -->

Events:
- 'lead:updated' → Lead object (after PATCH /leads/:id)
- 'lead:moved' → { leadId: string, stageId: string } (after drag-and-drop move)
- 'lead:handoff' → { leadId: string, humanOverride: boolean } (after handoff toggle)
- 'lead:note-added' → LeadNote object (after POST /leads/:id/notes)
- 'message:new' → Message object (after POST /conversations/:id/message)

<!-- getSocket from frontend/src/lib/socket.ts -->
```typescript
export function getSocket(): Socket; // singleton, autoConnect: false
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useSocket hook + wire real-time events into Kanban and Inbox</name>
  <files>frontend/src/hooks/useSocket.ts, frontend/src/components/kanban/KanbanBoard.tsx, frontend/src/components/inbox/ChatWindow.tsx, frontend/src/app/inbox/page.tsx</files>
  <action>
1. Create frontend/src/hooks/useSocket.ts ('use client'):
   - Import getSocket from lib/socket, useEffect from react, useQueryClient from @tanstack/react-query
   - Export useSocket() hook:
     - On mount: call getSocket().connect()
     - Subscribe to events:
       - 'lead:updated' → invalidate queries ['leads'], ['lead', payload.id]
       - 'lead:moved' → invalidate queries ['leads']
       - 'lead:handoff' → invalidate queries ['leads'], ['conversations']
       - 'lead:note-added' → invalidate queries ['lead', payload.leadId]
       - 'message:new' → invalidate queries ['messages', payload.conversationId], ['conversations']
     - On unmount: remove all listeners (sock.off for each event), but do NOT disconnect (singleton stays alive)
   - This is the single integration point — all components get real-time updates via TanStack Query cache invalidation

2. Update frontend/src/components/kanban/KanbanBoard.tsx:
   - Import and call useSocket() at top of component
   - This makes the Kanban board reactive: when agent moves a lead or another user drags a card, TanStack Query refetches and the board updates
   - Remove any manual polling if added

3. Update frontend/src/app/inbox/page.tsx:
   - Import and call useSocket() at top of component
   - This makes the inbox reactive: new messages and handoff changes appear without polling

4. Update frontend/src/components/inbox/ChatWindow.tsx:
   - Remove refetchInterval: 5000 from the messages useQuery (no longer needed — Socket.IO push replaces polling)
   - The useSocket hook in the parent page handles invalidation
  </action>
  <verify>
    <automated>cd "/Users/softsales/Documents/SEVYN LABS/CODE/autoscar-agent/frontend" && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>useSocket hook connects to Socket.IO and invalidates TanStack Query caches on events. KanbanBoard and inbox pages call useSocket. ChatWindow no longer polls. All UI updates happen in real time via WebSocket push.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 3 CRM + Multichat delivery</name>
  <action>Human verifies the complete Phase 3 delivery end-to-end</action>
  <what-built>Complete CRM + Multichat Phase: Kanban board with drag-and-drop, multichat inbox with human handoff, pipeline settings, real-time WebSocket updates. Full Phase 3 delivery.</what-built>
  <how-to-verify>
    1. Run `docker-compose up --build` from project root
    2. Open http://localhost in browser
    3. KANBAN (CRM-01, CRM-04, CRM-06):
       - Navigate to /crm — verify Kanban board shows pipeline stages as columns
       - If leads exist, verify cards appear in correct columns
       - Drag a card from one column to another — verify it stays in the new column after refresh
       - Use search input — verify filtering by name/phone works
       - Click a lead card — verify detail sheet opens with Conversa, Notas, Dados tabs
       - Edit lead data in Dados tab — verify form saves successfully
    4. INBOX (WAPP-05, WAPP-06):
       - Navigate to /inbox — verify conversation list appears
       - Click a conversation — verify message history loads
       - Type and send a message — verify it appears in chat and is sent via WhatsApp
       - Click "Assumir conversa" — verify badge changes to "Operador ativo"
       - Send a WhatsApp message to the connected number — verify AI does NOT respond (humanOverride active)
       - Click "Devolver para IA" — verify AI resumes responding
    5. SETTINGS (CRM-02, CRM-03):
       - Navigate to /settings/pipeline — verify stages are listed
       - Add a new stage — verify it appears in the list
       - Rename a stage — verify the change persists
       - Add a qualification rule — verify it appears in the rules list
       - Delete a rule — verify it is removed
    6. REAL-TIME (CRM-07):
       - Open two browser tabs on /crm
       - Drag a card in tab 1 — verify tab 2 updates without refresh
  </how-to-verify>
  <verify>Human confirms all 6 verification areas pass</verify>
  <done>All Phase 3 success criteria verified: Kanban DnD, multichat inbox, human handoff, pipeline config, qualification rules, real-time updates</done>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
TypeScript compiles. Socket.IO connection established on page load. Events trigger query invalidation. All 5 success criteria from ROADMAP verified by human.
</verification>

<success_criteria>
- Socket.IO events update Kanban board and inbox in real time (no polling)
- Human verifies all Phase 3 success criteria from ROADMAP:
  1. Kanban with drag-and-drop
  2. Qualification rules configurable per pipeline
  3. Search/filter leads + conversation history
  4. Manual lead edits + real-time sync
  5. Human handoff + multichat inbox
</success_criteria>

<output>
After completion, create `.planning/phases/03-crm-multichat/03-05-SUMMARY.md`
</output>
